import { ErrorDisplayDirective } from './error-display.directive';

describe('ErrorDisplayDirective', () => {
  it('should create an instance', () => {
    const directive = new ErrorDisplayDirective();
    expect(directive).toBeTruthy();
  });
});
